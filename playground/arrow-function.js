/*const square = (x) => {
    return x*x;
}*/


//const square = (x) => x*x

//console.log(square(3))
/*
const event = {
    name: 'Birthday party',
    guestList: ['chandini', 'nrupen'],
    printGuestList: function ()  {
        console.log('Guest list for '+this.name)
    },
    printGuests() {
        console.log(this.name)
    },

    printGuestsAttending() {
        this.guestList.forEach((guest) => {
            console.log(`The guest ${guest} is attending ${this.name}`);
        })
    }

}

event.printGuestsAttending()


event.printGuestList()
event.printGuests()
*/